# Proposal Logs

This folder is the default proposal-log surface for repos that adopt the
proposal protocol.

Use explicit status buckets:

- `active/`
- `pending/`
- `blocked/`
- `in-progress/`
- `archive/`
